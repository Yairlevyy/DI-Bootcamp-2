select count(*) from actor

-- 200 Actors

select * from actor

insert into actor (first_name,last_name)
values('Jhon','Doe')

-- It's works, but since we didn't do the actor table in class i used this one, but i supposed that the one in the class had constraint not null and if it's the case that will return an error.